<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/dialogListFragment">
    <fragment
        android:id="@+id/dialogListFragment"
        android:name="com.example.wb_4.presentation.dialog_list.DialogListFragment"
        android:label="fragment_dialog_list"
        tools:layout="@layout/fragment_dialog_list" >
        <action
            android:id="@+id/action_dialogListFragment_to_chatFragment"
            app:destination="@id/chatFragment" />
    </fragment>
    <fragment
        android:id="@+id/chatFragment"
        android:name="com.example.wb_4.presentation.chat.ChatFragment"
        android:label="fragment_chat"
        tools:layout="@layout/fragment_chat" />
</navigation>